# JadHor OS - AI Context Transfer & Architecture Guide

เอกสารฉบับนี้รวบรวม **"ตรรกะความคิด กฎเกณฑ์ ดีไซน์ระบบ และการตัดสินใจในการแก้ปัญหา"** ที่สั่งสมมาตลอดการพัฒนาโปรเจกต์ JadHor OS เพื่อให้คุณสามารถคัดลอกไฟล์นี้ (หรือใช้สอน AI ในโปรเจกต์ถัดไป) เพื่อให้ AI เข้าใจและคงมาตรฐานเดิมได้ทันที

---

## 1. การย้ายข้ามระบบความจำ (How to Migrate AI Brain)

เพื่อให้ AI ในโปรเจกต์ใหม่รับรู้ความทรงจำเดิม ให้ดำเนินการคัดลอกหรือสร้างไฟล์เหล่านี้ที่รูทของโปรเจกต์ใหม่:

1.  **`.agents/AGENTS.md` (ไฟล์สำคัญที่สุด):** บันทึกกฎเหล็ก โทนสี อัตลักษณ์ของแบรนด์ และกฎโครงสร้างพื้นฐานที่ไม่ยอมให้ AI ละเมิด
2.  **`MEMORY.md`:** เขียนสรุปสถานะปัจจุบันของแอปพลิเคชัน เทคโนโลยีที่เลือกใช้ และแนวคิดของโปรเจกต์ใหม่ลงไป เพื่อให้ AI เริ่มต้นทำงานต่อได้ทันที
3.  **การใช้คำสั่ง `/learn` (ในช่องแชท):** เมื่อ AI ทำงานได้ตามแนวทางที่ต้องการแล้ว พิมพ์คำสั่ง `/learn` เพื่อล็อกชุดพฤติกรรมนี้ไว้ในฐานความรู้ส่วนกลางถาวร

---

## 2. บันทึกข้อผิดพลาดในอดีตและแนวทางการแก้ไข (Lessons Learned & Error Avoidance)

| ปัญหาที่เคยเกิด (Mistakes) | แนวทางแก้ไขถาวร (Corrections) |
|---|---|
| **Next.js Static Generation Error ในการรัน Test:** การทดสอบด้วย Vitest โยนข้อผิดพลาด `revalidatePath missing static generation store` | **แก้โดยการ Mock Caching:** เติม `vi.mock("next/cache", () => ({ revalidatePath: vi.fn() }))` ไว้ที่หัวไฟล์ทดสอบทุกครั้งเพื่อจำลอง Next.js Server Environment |
| **Prisma Mocking Relations ล้มเหลว:** เมื่อ mock คืนค่า Prisma data เป็นก้อนแบนๆ แต่ Server Actions เรียกใช้ `include` ทำให้เจอ `TypeError: Cannot read properties of undefined` | **แก้โดยการจำลอง Nested Objects:** ในไฟล์ทดสอบ ต้องจำลองข้อมูลลึกถึงระดับที่ฟังก์ชันจริงเรียกใช้ เช่น `tenant: { userId: "..." }` หรือ `room: { property: { ... } }` |
| **Vercel Cron ทำงานไม่ตรง Timezone:** ตั้ง Cron ไปทำงานในโซนเวลาที่ไม่เสถียร หรือหลุด Region | **แก้โดยการกำหนด UTC + sin1:** ต้องคงดีไซน์ `"regions": ["sin1"]` ใน `vercel.json` เพื่อให้เซิร์ฟเวอร์อยู่ใกล้ Supabase (สิงคโปร์) เสมอ และบวกเวลาไปล่วงหน้า 7 ชั่วโมงสำหรับ Cron โซนเวลาไทย เช่น (03:00 UTC = 10:00 ICT) |
| **การส่งข้อมูลทับซ้อน (Approved Guard):** ผู้เช่ากรอกตัวเลขใหม่ทับตัวเลขเดิมหลังจากบิลได้รับการอนุมัติ (APPROVED) ไปแล้ว | **แก้โดยการล็อกสถานะ:** ตรวจสอบสถานะเดิมก่อนยอมรับข้อมูลลง DB หากสถานะเป็น `APPROVED` แล้ว จะปฏิเสธการอัปเดตข้อมูลทุกกรณี |
